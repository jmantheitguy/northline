services:
  orbit:
    build: .
    container_name: orbit-project-hub
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - ORBIT_DATA_DIR=/app/data
      - ORBIT_ADMIN_EMAIL=${ORBIT_ADMIN_EMAIL:-admin}
      - ORBIT_ADMIN_PASSWORD=${ORBIT_ADMIN_PASSWORD:-password}
    volumes:
      - orbit-data:/app/data
volumes:
  orbit-data:
