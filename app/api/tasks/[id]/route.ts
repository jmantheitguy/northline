import { NextResponse } from "next/server";
import { currentUser } from "@/lib/auth";
import db from "@/lib/db";
import { boardPermission,canEdit } from "@/lib/boards";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const user=await currentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const id=Number((await params).id),task=db.prepare("SELECT board_id FROM tasks WHERE id=?").get(id) as {board_id:number}|undefined;if(!task)return NextResponse.json({error:"Not found"},{status:404});if(!canEdit(boardPermission(user,task.board_id)))return NextResponse.json({error:"Forbidden"},{status:403});const body=await request.json();const allowed=["title","description","status","priority","tag","due_date","assignee_id"] as const;for(const key of allowed)if(Object.prototype.hasOwnProperty.call(body,key))db.prepare(`UPDATE tasks SET ${key}=?,updated_at=CURRENT_TIMESTAMP WHERE id=?`).run(body[key],id);db.prepare("UPDATE boards SET updated_at=CURRENT_TIMESTAMP WHERE id=?").run(task.board_id);return NextResponse.json({ok:true});}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const user=await currentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const id=Number((await params).id),task=db.prepare("SELECT board_id FROM tasks WHERE id=?").get(id) as {board_id:number}|undefined;if(!task)return NextResponse.json({error:"Not found"},{status:404});if(!canEdit(boardPermission(user,task.board_id)))return NextResponse.json({error:"Forbidden"},{status:403});db.prepare("DELETE FROM tasks WHERE id=?").run(id);return NextResponse.json({ok:true});}
