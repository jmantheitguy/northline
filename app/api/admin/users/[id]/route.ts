import { NextResponse } from "next/server";
import db from "@/lib/db";
import { requireAdmin } from "@/lib/auth";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const admin=await requireAdmin();if(!admin)return NextResponse.json({error:"Forbidden"},{status:403});const id=Number((await params).id);if(id===admin.id)return NextResponse.json({error:"You cannot suspend your own account"},{status:400});const {status,role}=await request.json();if(status&&!['Active','Suspended'].includes(status))return NextResponse.json({error:"Invalid status"},{status:400});if(role&&!['Admin','Member','Guest'].includes(role))return NextResponse.json({error:"Invalid role"},{status:400});if(status)db.prepare("UPDATE users SET status=? WHERE id=?").run(status,id);if(role)db.prepare("UPDATE users SET role=? WHERE id=?").run(role,id);db.prepare("INSERT INTO audit_log(actor_id,action,target) VALUES(?,?,?)").run(admin.id,"USER.UPDATE",String(id));return NextResponse.json({ok:true});}
